---
"@eins78/agent-skills": minor
---

Polish pass on `dossier` + `ballot` (PR #43 post-review):

- **Ballot reframed as async-decision instrument.** Trigger is async decision-making (review over chat / on a PR / on a train / remote handoff), not reviewer count. Single-decider async is valid; multi-reviewer panels still get per-reviewer files. SKILL.md description, README, conventions, template, marketplace entry, and dossier cross-reference all updated to match.
- **Six overfit audit hooks removed.** `dossier-citation-audit.sh`, `dossier-forbidden-words.sh`, `dossier-section-order.sh`, `dossier-dated-claim-scan.sh`, `ballot-anti-option.sh`, `ballot-cover-archaeology.sh`. Each encoded a11y-session-specific patterns that didn't generalize (the `[Xn]` citation style, OSS-mode wordlist, H2-level glossary heading, specific archaeology phrases). See Pitch-A assessment §§3.1 and 3.3 for the original concern.
- **Two hooks kept** (`ballot-filename.sh`, `dossier-framing-declared.sh`) — mechanical checks that are pattern-reliable across dossier styles. The dispatcher (`dossier-hook-dispatcher.sh`) kept and simplified; it now only shims JSON-stdin to argv for the two scripts.
- **Reviewer checklists replace the grep gates.** `skills/dossier/references/review-checklist.md` (8 items, ~180 lines) covers framing coherence, citation integrity, dated-claim freshness, section ordering, source bias flagging, hyperlink density, selectivity, and Key Facts box accuracy. `skills/ballot/references/review-checklist.md` (8 items, ~150 lines) covers filename pattern, cover-block cleanliness, anti-options, time-horizon-per-DEC, recommended-but-not-pre-ticked, tier discipline, async-readability, and reconciliation location. Each item structured as what-to-check / why / good / red-flags.
- **`framing-modes.yaml` deleted** (orphaned once `forbidden-words.sh` was removed; no remaining consumer). `framing-modes.md` retained as mode-selection guidance and expanded with inline vocabulary examples that were previously in the YAML.
- **`audit-checks.md` deleted** (80% of its content documented deleted scripts; the two kept hooks are self-documenting in one line each in their respective SKILL.md §Gates sections).

Minor bump for both skills: ballot reframe is user-visible semantic expansion (single async decider now supported as a first-class use case); dossier gains the review-checklist as a new review mechanism. No breaking changes — existing ballot and dossier files remain valid.

<!--
bumps:
  skills:
    ballot: minor
    dossier: minor
-->
